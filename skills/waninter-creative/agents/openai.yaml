interface:
  display_name: "Waninter Creative"
  short_description: "Generate images and videos with API keys"
  default_prompt: "Use $waninter-creative to configure my API key and generate an image or video from my prompt."
policy:
  allow_implicit_invocation: true
